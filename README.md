To install dependencies:
```sh
bun install
```

To run:
```sh
bun run dev
```

open http://localhost:3000

example query (enter in address bar): http://localhost:3000/?chatstr=what is the best way to spend my time this weekend?

Also prints nice formatted output of the conversation to the console while running. 

No frontend as of yet.
